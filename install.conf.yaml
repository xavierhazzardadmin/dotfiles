- defaults:
      link:
          relink: true

- clean: ['~']

- link:
    ~/.config/ ./.config
    ~/.zshrc ./.zshrc
    ~/.xinitrc ./.xinitrc

- shell:
    - [git submodule update --init --recursive, Installing submodules]
